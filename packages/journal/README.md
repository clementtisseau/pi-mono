# @clementtisseau/pi-journal

Repo-local journaling for Pi coding agent activity.

This MVP captures Pi events, groups them into change units, stores them in a local SQLite database under `.git/pi-journal/`, and exposes a minimal Pi extension with a `/changes` command and a small status widget.
